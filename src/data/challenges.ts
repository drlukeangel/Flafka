import type { Challenge } from '../types/learn';

export const challenges: Challenge[] = [
  // --- Track 1: Getting Started (5 challenges) ---
  {
    id: 'challenge-hello-flink-1',
    exampleId: 'hello-flink',
    prompt: 'Modify the query to select only the first two columns instead of all columns.',
    hint: 'Replace SELECT * with explicit column names.',
    expectedBehavior: 'The output should display only the two specified columns for each row.',
  },
  {
    id: 'challenge-good-jokes-1',
    exampleId: 'good-jokes',
    prompt: 'Add a WHERE clause to filter jokes that contain a specific keyword of your choice.',
    hint: 'Use the LIKE operator with a % wildcard pattern.',
    expectedBehavior: 'Only jokes containing your keyword appear in the results.',
  },
  {
    id: 'challenge-loan-filter-1',
    exampleId: 'loan-filter',
    prompt: 'Change the filter to find loans with an amount between 100,000 and 500,000.',
    hint: 'Use the BETWEEN operator or combine two comparisons with AND.',
    expectedBehavior: 'Only loans within the specified amount range are returned.',
  },
  {
    id: 'challenge-loan-filter-2',
    exampleId: 'loan-filter',
    prompt: 'Add an ORDER BY clause to sort the filtered results by loan amount descending.',
    hint: 'Append ORDER BY amount DESC to the query.',
    expectedBehavior: 'Results appear sorted with the largest loan amounts first.',
  },
  {
    id: 'challenge-loan-unnest-1',
    exampleId: 'loan-coborrower-unnest',
    prompt: 'After unnesting co-borrowers, add a filter to only show co-borrowers with a credit score above 700.',
    hint: 'Add a WHERE clause referencing the unnested co-borrower credit score column.',
    expectedBehavior: 'Only co-borrower rows with credit scores above 700 appear in the output.',
  },

  // --- Track 2: Kafka Fundamentals (6 challenges) ---
  {
    id: 'challenge-kafka-produce-1',
    exampleId: 'kafka-produce-consume',
    prompt: 'Insert three new records into the topic using INSERT INTO with different values.',
    hint: 'Use INSERT INTO table_name VALUES (...), (...), (...) syntax.',
    expectedBehavior: 'Three new records appear in the topic when you query it.',
  },
  {
    id: 'challenge-kafka-startup-1',
    exampleId: 'kafka-startup-modes',
    prompt: 'Change the startup mode to earliest-offset and compare the number of records with latest-offset.',
    hint: "Set 'scan.startup.mode' = 'earliest-offset' in the table options.",
    expectedBehavior: 'With earliest-offset you see all historical records; with latest-offset you see only new records.',
  },
  {
    id: 'challenge-kafka-changelog-1',
    exampleId: 'kafka-changelog-modes',
    prompt: 'Create a simple GROUP BY query and observe the changelog output (inserts and updates).',
    hint: 'Try SELECT key, COUNT(*) FROM your_table GROUP BY key and watch the +I and -U/+U messages.',
    expectedBehavior: 'You see initial inserts followed by retract/update pairs as counts change.',
  },
  {
    id: 'challenge-kafka-changelog-2',
    exampleId: 'kafka-changelog-modes',
    prompt: 'Write the changelog output to a new Kafka topic using upsert mode.',
    hint: 'Create a table with a PRIMARY KEY and use INSERT INTO to write the aggregation results.',
    expectedBehavior: 'The downstream topic contains the latest aggregated value per key.',
  },
  {
    id: 'challenge-kafka-formats-1',
    exampleId: 'kafka-value-formats',
    prompt: 'Create a new table that reads the same topic but with a different value format configuration.',
    hint: "Try changing the 'value.format' property in the WITH clause.",
    expectedBehavior: 'The table reads data using the new format specification.',
  },
  {
    id: 'challenge-kafka-formats-2',
    exampleId: 'kafka-value-formats',
    prompt: 'Query metadata columns like partition, offset, and timestamp from the Kafka topic.',
    hint: 'Add metadata columns to your table DDL using the METADATA keyword.',
    expectedBehavior: 'Each row displays its Kafka partition number, offset, and ingestion timestamp.',
  },

  // --- Track 3: Windowing & Time (8 challenges) ---
  {
    id: 'challenge-loan-agg-1',
    exampleId: 'loan-aggregate',
    prompt: 'Change the tumble window size from the current duration to 5 minutes and observe how group sizes change.',
    hint: 'Modify the INTERVAL in the TUMBLE function call.',
    expectedBehavior: 'Window groups become larger as the 5-minute window captures more events.',
  },
  {
    id: 'challenge-loan-agg-2',
    exampleId: 'loan-aggregate',
    prompt: 'Add AVG(amount) and MAX(amount) to the aggregation alongside the existing COUNT.',
    hint: 'Add the aggregate functions to the SELECT list.',
    expectedBehavior: 'Each window result now includes count, average, and maximum loan amounts.',
  },
  {
    id: 'challenge-hop-window-1',
    exampleId: 'loan-hop-window',
    prompt: 'Experiment with the hop window by setting the slide to 1 minute and the size to 10 minutes.',
    hint: 'HOP(TABLE t, DESCRIPTOR(ts), INTERVAL \'1\' MINUTE, INTERVAL \'10\' MINUTE).',
    expectedBehavior: 'Each event appears in up to 10 overlapping windows, producing more frequent output.',
  },
  {
    id: 'challenge-session-window-1',
    exampleId: 'loan-session-window',
    prompt: 'Change the session gap to 2 minutes and compare the number of sessions with the original gap.',
    hint: 'Modify the INTERVAL parameter in the SESSION function.',
    expectedBehavior: 'A shorter gap produces more sessions; a longer gap merges nearby events into fewer sessions.',
  },
  {
    id: 'challenge-cumulate-window-1',
    exampleId: 'loan-cumulate-window',
    prompt: 'Set the cumulate step to 1 minute and the max size to 1 hour. Add a SUM aggregate.',
    hint: 'CUMULATE(TABLE t, DESCRIPTOR(ts), INTERVAL \'1\' MINUTE, INTERVAL \'1\' HOUR).',
    expectedBehavior: 'Cumulative totals grow every minute within each hourly window period.',
  },
  {
    id: 'challenge-late-payments-1',
    exampleId: 'loan-late-payments',
    prompt: 'Adjust the watermark delay to a larger value and observe whether fewer events are dropped as late.',
    hint: 'Change the INTERVAL in the WATERMARK FOR clause in the table DDL.',
    expectedBehavior: 'More late-arriving events are included in window results with a more generous watermark.',
  },
  {
    id: 'challenge-time-range-1',
    exampleId: 'loan-time-range-stats',
    prompt: 'Add a HAVING clause to only output windows where the count exceeds a threshold.',
    hint: 'Add HAVING COUNT(*) > 5 after the GROUP BY clause.',
    expectedBehavior: 'Only windows with more than the threshold number of events appear in results.',
  },
  {
    id: 'challenge-time-range-2',
    exampleId: 'loan-time-range-stats',
    prompt: 'Combine two different window types (e.g., tumble inside a view, hop in the outer query).',
    hint: 'Create a view with one window type, then query that view with another window aggregation.',
    expectedBehavior: 'The output reflects the composition of both windowing strategies.',
  },

  // --- Track 4: Joins & Enrichment (6 challenges) ---
  {
    id: 'challenge-loan-join-1',
    exampleId: 'loan-join',
    prompt: 'Change the INNER JOIN to a LEFT JOIN and observe what happens when there is no match on the right side.',
    hint: 'Replace INNER JOIN with LEFT JOIN. Unmatched rows will have NULL values for the right side.',
    expectedBehavior: 'All left-side records appear; unmatched rows show NULLs for right-side columns.',
  },
  {
    id: 'challenge-temporal-join-1',
    exampleId: 'loan-temporal-join',
    prompt: 'Query the versioned table directly to see its changelog, then compare with the temporal join output.',
    hint: 'Run SELECT * FROM the versioned table to see all versions, then compare timestamps.',
    expectedBehavior: 'The temporal join picks the correct version at each event time, not the latest.',
  },
  {
    id: 'challenge-interval-join-1',
    exampleId: 'loan-interval-join',
    prompt: 'Narrow the interval window to 5 minutes and observe how many matches are lost.',
    hint: 'Change the BETWEEN interval to INTERVAL \'5\' MINUTE.',
    expectedBehavior: 'Fewer matches are found because events outside the 5-minute window are excluded.',
  },
  {
    id: 'challenge-interval-join-2',
    exampleId: 'loan-interval-join',
    prompt: 'Make the interval asymmetric: allow 10 minutes before but only 1 minute after.',
    hint: 'Use BETWEEN a.ts - INTERVAL \'10\' MINUTE AND a.ts + INTERVAL \'1\' MINUTE.',
    expectedBehavior: 'The join favors events that arrived before the left side, with minimal tolerance for later events.',
  },
  {
    id: 'challenge-enrichment-1',
    exampleId: 'loan-stream-enrichment',
    prompt: 'Add a COALESCE to handle NULL values from the enrichment side when no match exists.',
    hint: "Use COALESCE(enriched.field, 'unknown') in the SELECT list.",
    expectedBehavior: 'Instead of NULLs, unmatched enrichment fields show the default value you specified.',
  },
  {
    id: 'challenge-property-lookup-1',
    exampleId: 'loan-property-lookup',
    prompt: 'Add a computed column that calculates the loan-to-value (LTV) ratio using the looked-up property value.',
    hint: 'Add loan.amount / property.value AS ltv_ratio to the SELECT list.',
    expectedBehavior: 'Each row shows the LTV ratio computed from the loan amount and looked-up property value.',
  },

  // --- Track 5: Stateful Processing (7 challenges) ---
  {
    id: 'challenge-dedup-1',
    exampleId: 'loan-dedup',
    prompt: 'Modify the deduplication to keep the LAST record per key instead of the first.',
    hint: 'Change ORDER BY event_time ASC to ORDER BY event_time DESC.',
    expectedBehavior: 'For each key, the most recent record is retained instead of the earliest.',
  },
  {
    id: 'challenge-cdc-1',
    exampleId: 'loan-cdc-pipeline',
    prompt: 'Add a filter to the CDC pipeline to only propagate UPDATE and DELETE operations, skipping INSERTs.',
    hint: 'Use metadata columns or op field to filter by operation type.',
    expectedBehavior: 'Only update and delete changes are forwarded downstream; new inserts are ignored.',
  },
  {
    id: 'challenge-running-agg-1',
    exampleId: 'loan-running-aggregate',
    prompt: 'Add a running average alongside the running total and observe how it converges over time.',
    hint: 'Use AVG(amount) OVER the same window specification.',
    expectedBehavior: 'The running average stabilizes as more events are processed, while the total keeps growing.',
  },
  {
    id: 'challenge-running-agg-2',
    exampleId: 'loan-running-aggregate',
    prompt: 'Set table.exec.state.ttl to 1 minute, run the query, and observe what happens when state expires.',
    hint: "SET 'table.exec.state.ttl' = '60s'; before your query.",
    expectedBehavior: 'Running aggregates reset for keys that have been idle for more than 1 minute.',
  },
  {
    id: 'challenge-change-detect-1',
    exampleId: 'loan-change-detection',
    prompt: 'Modify the change detection to only alert when the status changes from a specific value to another.',
    hint: 'Add a WHERE clause comparing the previous and current status values.',
    expectedBehavior: 'Only the specific status transition you defined triggers an output row.',
  },
  {
    id: 'challenge-pattern-match-1',
    exampleId: 'loan-pattern-match',
    prompt: 'Add a third step to the MATCH_RECOGNIZE pattern to detect a three-event sequence.',
    hint: 'Extend the PATTERN clause from (A B) to (A B C) and define condition C.',
    expectedBehavior: 'Only three-event sequences matching all three conditions produce output.',
  },
  {
    id: 'challenge-borrower-payments-1',
    exampleId: 'loan-borrower-payments',
    prompt: 'Add a payment streak counter that tracks consecutive on-time payments per borrower.',
    hint: 'Use a CASE expression with LAG to compare each payment status with the previous one.',
    expectedBehavior: 'Each borrower shows a streak count that resets when a payment is late.',
  },

  // --- Track 6: Views & Architecture (5 challenges) ---
  {
    id: 'challenge-golden-record-1',
    exampleId: 'view-golden-record',
    prompt: 'Add a new source to the golden record by joining in a third stream of data.',
    hint: 'Add another JOIN clause to the view definition with the new source table.',
    expectedBehavior: 'The golden record now includes fields from three upstream sources.',
  },
  {
    id: 'challenge-credit-risk-1',
    exampleId: 'view-credit-risk',
    prompt: 'Add a risk category label (LOW, MEDIUM, HIGH) based on the computed risk score using a CASE expression.',
    hint: 'Use CASE WHEN score < 30 THEN \'LOW\' WHEN score < 70 THEN \'MEDIUM\' ELSE \'HIGH\' END.',
    expectedBehavior: 'Each row includes a human-readable risk category alongside the numeric score.',
  },
  {
    id: 'challenge-ai-drift-1',
    exampleId: 'view-ai-drift',
    prompt: 'Add a threshold parameter so drift is only flagged when it exceeds a configurable percentage.',
    hint: 'Add a WHERE clause or CASE expression comparing the drift metric to a threshold constant.',
    expectedBehavior: 'Only drift events exceeding the threshold are flagged in the output.',
  },
  {
    id: 'challenge-early-warning-1',
    exampleId: 'view-early-warning',
    prompt: 'Add a severity level to the early warning system based on how many warning signals are active simultaneously.',
    hint: 'Count the number of active warning conditions and map to severity levels.',
    expectedBehavior: 'Each warning includes a severity level (e.g., WATCH, WARNING, CRITICAL) based on signal count.',
  },
  {
    id: 'challenge-dynamic-routing-1',
    exampleId: 'loan-routing-json',
    prompt: 'Add a new routing rule that sends loans above a certain amount to a special high-value topic.',
    hint: 'Upsert a new routing rule in ROUTING-RULES with target_topics pointing to a new destination table.',
    expectedBehavior: 'High-value loans are routed to the designated topic while other rules still apply.',
  },

  // --- Track 7: Confluent Cloud (5 challenges) ---
  {
    id: 'challenge-schemaless-1',
    exampleId: 'loan-schemaless-topic',
    prompt: 'Parse a nested JSON field from the raw payload using JSON_VALUE or JSON_QUERY.',
    hint: "Use JSON_VALUE(raw_value, '$.nested.field') in the SELECT list.",
    expectedBehavior: 'The nested field is extracted and displayed as a column in the query results.',
  },
  {
    id: 'challenge-schema-override-1',
    exampleId: 'loan-schema-override',
    prompt: 'Override the schema to rename a column and cast its type from STRING to INT.',
    hint: 'Define the column with the new name and type in the CREATE TABLE DDL.',
    expectedBehavior: 'The column appears with the new name and type, with values successfully cast.',
  },
  {
    id: 'challenge-schema-evolution-1',
    exampleId: 'kafka-schema-evolution',
    prompt: 'Add a new optional field with a default value and verify it is backward compatible.',
    hint: 'Add the field to the schema with a default value, then register the new version.',
    expectedBehavior: 'The new schema version is accepted by Schema Registry and old consumers can still read data.',
  },
  {
    id: 'challenge-connector-bridge-1',
    exampleId: 'confluent-connector-bridge',
    prompt: 'Query the connector-managed topic and join it with an existing stream to build an enriched view.',
    hint: 'Create a Flink SQL table pointing to the connector topic, then JOIN it with another table.',
    expectedBehavior: 'The enriched output combines data from the connector source with the streaming data.',
  },
  {
    id: 'challenge-connector-bridge-2',
    exampleId: 'confluent-connector-bridge',
    prompt: 'Write filtered results from a Flink SQL query into a topic that a sink connector reads from.',
    hint: 'Use INSERT INTO sink_topic SELECT ... FROM ... WHERE ... to feed the connector.',
    expectedBehavior: 'Filtered records appear in the sink topic and are delivered to the external system by the connector.',
  },
];
